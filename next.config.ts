import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nothing exotic on purpose. This kit is meant to be readable and to drop
  // into an existing app without dragging configuration along with it.
};

export default nextConfig;
